apiVersion: route.openshift.io/v1
kind: Route
metadata:
  creationTimestamp: null
  name: cart
  namespace: dev
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: inventory
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
  wildcardPolicy: None